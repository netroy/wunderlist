define('models/list', ['models/base'], function(BaseModel) {

  //"use strict";

  //var
  window.ListModel = BaseModel.extend({
    'initialize': function() {

    },
    'uniqueId': function() {
      
    }
  });

  return ListModel;

});